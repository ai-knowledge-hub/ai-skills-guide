{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://skills.ai-knowledge-hub.org/schemas/smoke-evidence.schema.json",
  "title": "Skills Hub smoke evidence",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "contract_id",
    "contract_version",
    "evidence_scope",
    "test_id",
    "test_version",
    "producer",
    "entry_id",
    "entry_version",
    "artifact_sha256",
    "dependency_lock_sha256",
    "install_source",
    "module",
    "runtime",
    "platform",
    "command",
    "environment_id",
    "configuration_class",
    "coverage",
    "started_at",
    "observed_at",
    "expires_at",
    "result",
    "promotion_eligible"
  ],
  "properties": {
    "schema_version": { "const": "skills-hub.smoke-evidence/v1" },
    "contract_id": { "const": "operational-readiness" },
    "contract_version": { "const": "1.0.3" },
    "evidence_scope": { "const": "executable" },
    "test_id": { "const": "skills-hub/declared-smoke" },
    "test_version": { "const": "1" },
    "producer": { "const": "skills-hub" },
    "entry_id": { "type": "string", "pattern": "^[a-z0-9-]+/[a-z0-9-]+$" },
    "entry_version": { "type": "string", "minLength": 1 },
    "artifact_sha256": { "type": "string", "pattern": "^([a-f0-9]{64})?$" },
    "dependency_lock_sha256": {
      "type": "string",
      "pattern": "^(not-applicable|not-evaluated|[a-f0-9]{64})$"
    },
    "install_source": { "enum": ["local", "remote", "unknown"] },
    "module": { "enum": ["skills", "agents", "tools", "plugins"] },
    "runtime": { "enum": ["codex", "claude", "generic"] },
    "platform": { "enum": ["linux", "macos", "windows"] },
    "command": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "credential_binding_keys": {
      "type": "object",
      "propertyNames": { "pattern": "^[A-Z][A-Z0-9_]*$" },
      "additionalProperties": { "type": "string", "pattern": "^[1-9][0-9]*:[^\\r\\n]+$" }
    },
    "provider_principal": { "type": "string", "minLength": 1 },
    "provider_account": { "type": "string", "minLength": 1 },
    "provider_target_fingerprint": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "attestation_reference": { "type": "string", "minLength": 1 },
    "publication_class": {
      "enum": ["public-redacted-summary"]
    },
    "redaction_reason": { "type": "string", "minLength": 1 },
    "redacted_fields": {
      "type": "array",
      "items": {
        "enum": [
          "credential_binding_keys",
          "provider_principal",
          "provider_account",
          "provider_target_fingerprint",
          "attestation_reference",
          "granted_scopes",
          "environment_id"
        ]
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "granted_scopes": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "validator_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "execution_runtime_fingerprint": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "environment_id": {
      "oneOf": [
        { "type": "string", "pattern": "^[a-f0-9]{32}$" },
        { "const": "redacted" }
      ]
    },
    "configuration_class": { "type": "string", "minLength": 1 },
    "coverage": {
      "type": "array",
      "items": {
        "enum": [
          "install-receipt",
          "installed-tree",
          "declared-smoke-test",
          "authoritative-provider-identity"
        ]
      },
      "minItems": 3,
      "maxItems": 4,
      "uniqueItems": true,
      "allOf": [
        { "contains": { "const": "install-receipt" } },
        { "contains": { "const": "installed-tree" } },
        { "contains": { "const": "declared-smoke-test" } }
      ]
    },
    "started_at": { "type": "string", "format": "date-time" },
    "observed_at": { "type": "string", "format": "date-time" },
    "expires_at": { "type": "string", "format": "date-time" },
    "result": { "enum": ["passed", "failed"] },
    "promotion_eligible": { "type": "boolean" },
    "failure_reason": { "type": "string", "minLength": 1 }
  },
  "allOf": [
    {
      "if": { "properties": { "result": { "const": "passed" } }, "required": ["result"] },
      "then": {
        "required": ["execution_runtime_fingerprint"],
        "properties": {
          "command": { "minItems": 1 },
          "observed_at": { "type": "string", "format": "date-time" },
          "expires_at": { "type": "string", "format": "date-time" }
        }
      },
      "else": {
        "required": ["failure_reason"],
        "properties": { "promotion_eligible": { "const": false } }
      }
    },
    {
      "if": {
        "properties": { "configuration_class": { "pattern": "^authenticated:" } },
        "required": ["configuration_class"]
      },
      "then": {
        "required": [
          "credential_binding_keys",
          "provider_principal",
          "provider_account",
          "provider_target_fingerprint",
          "attestation_reference",
          "granted_scopes",
          "validator_sha256"
        ],
        "properties": {
          "coverage": {
            "contains": { "const": "authoritative-provider-identity" }
          }
        }
      },
      "else": {
        "properties": { "coverage": { "maxItems": 3 } }
      }
    },
    {
      "if": { "required": ["redacted_fields"] },
      "then": {
        "required": ["publication_class", "redaction_reason"],
        "properties": {
          "publication_class": { "const": "public-redacted-summary" },
          "promotion_eligible": { "const": false },
          "configuration_class": { "const": "redacted-summary" },
          "environment_id": { "const": "redacted" },
          "redacted_fields": {
            "minItems": 7,
            "maxItems": 7,
            "allOf": [
              { "contains": { "const": "credential_binding_keys" } },
              { "contains": { "const": "provider_principal" } },
              { "contains": { "const": "provider_account" } },
              { "contains": { "const": "provider_target_fingerprint" } },
              { "contains": { "const": "attestation_reference" } },
              { "contains": { "const": "granted_scopes" } },
              { "contains": { "const": "environment_id" } }
            ]
          },
          "coverage": {
            "maxItems": 3,
            "not": {
              "contains": { "const": "authoritative-provider-identity" }
            }
          }
        },
        "not": {
          "anyOf": [
            { "required": ["credential_binding_keys"] },
            { "required": ["provider_principal"] },
            { "required": ["provider_account"] },
            { "required": ["provider_target_fingerprint"] },
            { "required": ["attestation_reference"] },
            { "required": ["granted_scopes"] }
          ]
        }
      }
    },
    {
      "if": {
        "anyOf": [
          { "required": ["publication_class"] },
          { "required": ["redaction_reason"] },
          {
            "properties": { "environment_id": { "const": "redacted" } },
            "required": ["environment_id"]
          }
        ]
      },
      "then": { "required": ["redacted_fields"] }
    }
  ]
}
