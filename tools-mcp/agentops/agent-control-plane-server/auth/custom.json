{
  "schema_version": "skills-hub.auth-driver/v1",
  "credential_mode": "external-env",
  "method": "custom",
  "flow": "custom",
  "runtimes": ["codex", "claude", "generic"],
  "bootstrap": {"command": ["scripts/control_plane_auth_driver.mjs", "custom", "bootstrap"]},
  "credential": {"command": ["scripts/control_plane_auth_driver.mjs", "custom", "credential"]},
  "status": {"command": ["scripts/control_plane_auth_driver.mjs", "custom", "status"]}
}
