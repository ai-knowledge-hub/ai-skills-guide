# Agent Control Plane MCP Server

A practical MCP template for governed agent execution.

Use this when an agent needs to propose or execute actions but the platform must enforce policy outside the model. The server exposes policy checks, approval creation, action logging, and audit verification as separate capabilities.

This directory is a template, not a production server implementation. It defines the contract a production server should implement.
