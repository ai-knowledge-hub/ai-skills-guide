{
  "schema_version": "skills-hub.auth-driver/v1",
  "credential_mode": "external-env",
  "method": "bearer-token",
  "flow": "credential-binding",
  "runtimes": ["codex", "claude", "generic"],
  "bootstrap": {"command": ["scripts/meta_ads_auth_driver.mjs", "bearer-token", "bootstrap"]},
  "credential": {"command": ["scripts/meta_ads_auth_driver.mjs", "bearer-token", "credential"]},
  "status": {"command": ["scripts/meta_ads_auth_driver.mjs", "bearer-token", "status"]}
}
